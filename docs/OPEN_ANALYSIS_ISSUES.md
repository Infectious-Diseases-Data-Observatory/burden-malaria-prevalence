# Issues to resolve before a final analysis

Updated 17 September 2026. The [current primary specification](ANALYSIS_PLAN.md) uses seven separate age-band MAP models with **gamma=2**, cr bases (PfPR k=5; time k=6), 18 regional/annual confounders, and one fixed posterior-median **child HIV-incidence imputation**. Snow comparisons are supplementary; the [joint model document](PFPR_AGE_BAND_MODEL.md) records a historical sensitivity specification. These issues remain open; fitting the model does not resolve them. The [incidence model documentation](../R_cbh/hiv/README.md) describes validation, censoring and uncertainty propagation.


## Current reduced primary adjustment — 17 September 2026

The latest primary decision drops **Hib3, PCV, rotavirus and exclusive breastfeeding** from both the formula and complete-case filter. **Urban/rural residence remains required as the regional weighted urban percentage (`urban_pct`)**, with no missing assigned values in the current audit. The remaining 18 variables use regional summaries, the existing HIV imputation, UNICEF fallback for DTP3/measles and within-survey means of available regions for remaining regional gaps. Whole-survey and retained national-source gaps remain unresolved.

The [current audit](../results/cbh/regional_adjustment_reduced_v3/README.md) retains **5,680,117 child-band records, 1,755,838 children, 78,634 deaths, 973 survey-regions, 100 surveys and 34 countries** from all MAP-eligible records. The reduced specification has now been prepared and fitted as seven verified models in `primary_map_regional18_gamma2_v2`; the [comparison report](../results/cbh/primary_map_regional18_gamma2_v2/REPORT.md) covers the previous fitted iteration and national burden in 2005/2015/2024. Primary figures/tables, the inclusion flow and annual/state burden have now been regenerated from the revised fits. Sensitivity refits remain outstanding; manuscript table/caption text must be updated manually under the standing no-TeX-edit instruction. Historical vaccine/breastfeeding missingness below no longer excludes primary records. Those extraction issues remain relevant only if these variables are reintroduced in sensitivity analyses.

The new and old fitted samples share 5,617,520 child-band records; 267,502 old records are lost and 62,597 previously excluded records are recovered. Their effect differences combine changed covariate definitions, additional adjustment, substitution and sample selection. A common-sample comparison is needed to isolate adjustment changes, and further restricted fits would be needed to distinguish individual additions from aggregation. Filled regional estimates and fixed HIV values remain treated as known in the mortality model intervals.

The following two sections preserve the superseded 22-variable audits and their then-current imputation rules.

## Historical UNICEF national vaccination fallback — 17 September 2026

The [current pipeline](../R_cbh/covariates/README.md) now uses actual country/survey-year UNICEF WUENIC estimates for missing regional DTP3/measles values, preserving observed regional values and storing substitution flags. The three newer vaccines already use national band-entry-year values. With fallback, the candidate sample is **1,784,353 records, 598,386 children, 17,822 deaths and 419 survey-regions in 41 surveys/24 countries**. This recovers 927 records and one region relative to the pre-substitution audit. [Updated audit](../results/cbh/regional_adjustment_unicef_v2/README.md).

Unverified pre-series and no-series vaccine zeros still require introduction-history evidence; substituting from the same national dataset cannot resolve them. The other questionnaire and source issues below remain open. National substitution assumes the country estimate is a suitable proxy for an unobserved regional value; assess this through a no-substitution sensitivity and retain the source flags. Mortality models and manuscript figures have not been refitted.

## Historical expanded regional adjustment — 17 September 2026

The latest [analysis plan](ANALYSIS_PLAN.md#24-covariates-hiv-imputation-and-missing-data) supersedes the adjustment choices in the historical trial notes below: all confounders are regional summaries; all five vaccine measures, facility delivery, exclusive breastfeeding, short birth interval, WASH and electricity are selected. The [new extraction and audit](../R_cbh/covariates/README.md) leaves existing fitted models untouched.

The candidate complete-case sample has **1,783,426 records, 598,112 children, 17,815 deaths, 418 survey-regions, 41 surveys and 24 countries**. Relative to the previous fitted sample, **597/1,015 regions (58.8%) are lost**. [Detailed results](../results/cbh/regional_adjustment_v1/README.md) distinguish record missingness, partially reduced regions and whole-region loss.

Remaining work:

- **Historical vaccine coverage:** PCV and rotavirus are unavailable for 55.94% and 68.52% of previously fitted records. Most gaps are unverified pre-series/non-introduction placeholders. Verify introduction histories and true zero coverage separately from missing reporting; the current audit does not change these to zero. Complete-case restriction removes ten previously represented countries entirely.
- **Breastfeeding extraction:** generic feeding code can misread questionnaire skips and subsampling. Prefer published regional values; retain respondent/module flags and the national validation comparison. The audit leaves 71 previously included regions unavailable because of unresolved recode disagreements, in addition to 159 with no usable recode estimate. Ten survey-level checks require review, some already covered by usable published regional estimates. Two retained regions have fewer than 25 breastfeeding observations; 59 have fewer than 50. These remain available for the audit, but their uncertainty and a small-denominator sensitivity need assessment.
- **Published geography and timing:** checked aliases/version selectors resolve overlapping old/new region presentations, including DRC/Ghana/Senegal. Remaining absent regional values need distinguishing from source suppression and unavailable modules. Retain inclusion/exclusion clauses, birth/delivery recall windows and source age definitions. Do not fill a region from its country mean.
- **Regional estimand:** maternal education, wealth and urban residence are weighted means among distinct mothers with a recent birth; child characteristics use recent births once each. These are not general household/population averages. Regional summaries may leave individual confounding and have sampling error. Review transportability and timing before causal interpretation.
- **Historical refitting status:** the 22-variable candidate was not fitted. The later reduced 18-variable primary model has now been fitted in a separate version; see the current section above. Do not relabel the eleven-variable benchmark or the 22-variable availability audit as current fitted results.

The remaining sections preserve earlier evidence and trial decisions; vaccine exclusion and individual predictors stated there describe the **superseded** adjustment set.

## Country burden update: 9 September 2026

The [2024 national burden stage](../R_cbh/burden/README.md) uses the new finer-age IHME export. The remaining 2-4-year block is split using the user-authorized equal-rate/equal-person-time assumption. Its uncertainty is not included in the model intervals. The 0-27-day source group uses the <1-completed-month fitted PfPR effect; this boundary approximation remains explicit.

Population-weighted national PfPR now includes grid-cell area in the GPW 2020 density weights; the historical national extraction omitted cell area. These are 2024 MAP values weighted by a fixed 2020 all-age population surface. Eleven of the 42 estimable countries have MAP coverage below 95% of population weight within the available raster footprint; Eswatini has only 2.6%. Applying covered-area prevalence nationally is provisional. Cape Verde, Lesotho and São Tomé and Príncipe have no usable prevalence in this extraction. Missing areas are not set to zero. A full-coverage exposure surface, including the treatment of non-endemic areas, needs resolving before final national burden estimates. The older DHS regional exposure extraction has not been changed by this national-weight correction.

The PfPR-to-zero calculation retains negative attributable effects and flags zero-exposure extrapolation. Causal interpretation and transfer of the common fitted curves to countries outside the fitting sample remain assumptions. Current primary age-band intervals use model covariance conditional on a fixed median HIV imputation; they omit HIV-imputation, IHME/MAP source, survey-design, age-allocation and smoothing-parameter uncertainty. Earlier joint multiple-imputation outputs used a different uncertainty calculation. Country total deaths currently have point estimates only.

## Historical decisions before the regional-adjustment revision

- Seven completed-month bands: <1, 1–5, 6–11, 12–23, 24–35, 36–47 and 48–59.
- Entry during the 60 months before interview, with the full potential band ending by interview for both deaths and survivors.
- PfPR and annual external covariates assigned at band-entry year.
- Complete cases for other actual model variables after joining child incidence, the sole imputed covariate. As requested, vaccine covariates are excluded from this trial.
- Adjustment set: sex, multiple birth, birth order, maternal age at birth, maternal education, household wealth quintile, urban residence, log child HIV incidence, log GDP per capita, log health expenditure per capita and political stability. Effects vary by age band. Wealth is categorical; continuous confounders enter linearly after centering/scaling.
- Fit each age band separately with MAP exposure, gamma=2, PfPR cr/k=5 and calendar-time cr/k=6, plus separate survey, country and survey-versioned region random intercepts and variances. Use the unweighted conditional binomial likelihood with cloglog link and full-band-width offset. A survey-weighted final specification has not been chosen.

The **historical prevalence complete-case fit** retained **5,261,313 child-band records and 69,772 deaths**, in **33 countries, 102 surveys and 998 survey-specific regions**. This is not the incidence-adjusted sample; its sample accounting is written separately under `results/cbh/age_band_hiv_incidence_shared_time_v3/`. Complete cases refer to model inputs, not every optional field in the dataset. Records with missing unused vaccines or stratum metadata can remain in this trial.

The incidence-adjusted input retains **5,885,022 records and 82,415 deaths**, in **34 countries, 105 surveys and 1,015 survey-specific regions**. Nigeria contributes 737,012 records and Comoros 17,936. Liberia loses 131,239 formerly included records because its incidence series is unavailable; São Tomé and Príncipe remains excluded. A comparison with the historical fit combines a changed HIV measure with a changed sample; a common-sample comparison would be needed to isolate the covariate-definition effect.

## Primary implementation routing

`Rscript run_all.R --primary` now selects the current regional version through [run_regional.R](../R_cbh/primary/run_regional.R), with prepared data and models in `data/derived_cbh/models/primary_map_regional18_gamma2_v2/`. The new runner uses the saved regional overlay and HIV incidence, refits seven models, and exports contrasts, national estimates, diagnostics and iteration plots without TeX output. The previous primary and paper workflow remains in `primary_map_gamma2_v1`; some older sensitivity/burden defaults still point to gamma=1-era objects. Use explicit versioned inputs when migrating remaining figures and sensitivities.

## Data coverage and measurement

| Issue | Evidence / implication | Work needed | Current trial treatment |
|---|---|---|---|
| **HIV coverage** | In the historical prevalence panel, 774,565 of 6,431,363 eligible records (12.04%) were missing. Nigeria has numeric adolescent incidence and Comoros has only `<0.01`, with no child incidence series for either. Liberia and São Tomé and Príncipe have no usable both-sex adolescent incidence series. | Validate predictions by withholding entire countries' child histories, especially comparable African countries. Missingness may depend on information not represented in the model. Locate missing incidence data for Liberia and São Tomé and Príncipe. | Predict child incidence from the joint age-series model where adolescent information exists, preserving censoring and imputation flags. No region-only filling for a country with neither series. |
| **HIV covariate definition** | Incidence measures new infections, whereas the prior covariate measured the stock of children living with HIV. Both are national ages 0–14 measures, not specific to each mortality band. | Assess whether incidence adequately controls the intended HIV-related mortality confounding; retain prevalence as a separately identified sensitivity. | Use `log_hiv_incidence` at band-entry year as explicitly requested. Preserve the historical prevalence fit, but do not include prevalence or adolescent incidence in the active mortality formula. |
| **HIV imputation uncertainty** | Missing country series require borrowing a child/adolescent relationship across countries. Source values are modelled and correlated, and values below 0.01 are censored. | Check MCMC and country-held-out coverage, prior/temporal sensitivity and the Monte Carlo precision of pooled contrasts. Published source uncertainty requires separate treatment. | Joint censored log-rate model with separate calendar splines and AR(1) residuals. Posterior trajectories include country effects and serial dependence; mortality propagation conditions on numeric source estimates and fixed mortality smoothing parameters. It is not joint outcome-compatible imputation or survey-design inference. |
| **Vaccine coverage** | Missingness is 25.21% for Hib3, 58.17% for PCV and 70.42% for rotavirus after rejecting assumed pre-series/no-series zeros. Adding all three to the proposed covariates would retain 1,825,229 rows in 24 countries and 41 surveys. | Distinguish verified non-introduction from unavailable reporting and gaps. Decide which vaccine covariates belong in the adjustment set and how to recover valid historical coverage. | Excluded at the user's request, rather than imputed or assigned zero. |
| **Other covariates and complete-case selection** | Health expenditure is missing in 0.98%, political stability in 2.83%, wealth in 1.79% and maternal education in 0.05% of eligible rows. Entire surveys/countries can be lost. Complete-case analysis can change the target population and introduce selection bias. | Examine exclusion patterns by country, year, PfPR and age; distinguish unavailable years/fields from merge problems. Specify a missing-data strategy and assess robustness. | Require all eleven selected covariates; save per-survey selection and overlapping per-variable missing counts. |
| **Unresolved geographic matches** | 28 source labels in 17 surveys remain unmatched, affecting 71,904 eligible rows. Some are historical groupings or labels explicitly excluding cities; others are absent from the MAP cache. | Verify recode-to-polygon definitions and add justified mappings. Do not remove exclusion clauses or infer matches by elimination. | Retain audit records; exclude unmatched rows from fitting. |
| **MAP coverage and exact annual joins** | Four Lesotho surveys lack MAP geography entirely. Another 1,657 eligible rows have a known region but no exact annual PfPR match. Missing coverage can be concentrated in low-transmission areas. | Check extraction coverage, raster no-data versus genuine zero prevalence, and available years. | No zero substitution, interpolation or nearest-year borrowing. |
| **Alternative Snow prevalence** | The supplied annual re-fit covers 2000–2015 and 520 polygons; there are no polygons for Comoros, Lesotho or São Tomé and Príncipe. Some regions have low population coverage. The source README reports sparse observations in 2014–2015, no transmission-limits mask, maximum R-hat 1.0595 and 697 maximum-tree-depth events. | Assess exposure posterior convergence and sensitivity to coverage, sparse years and uncertainty. Aggregate posterior draws to obtain region intervals; marginal CSV quantiles cannot be averaged. | The first sensitivity uses population-weighted posterior means and exact band-entry-year joins, excludes post-2015 surveys/interviews and coverage below 50%, and refits MAP on the same records. Mortality intervals condition on Snow point estimates and one median HIV imputation. |
| **Regional identity across surveys** | Current region identifiers include survey/boundary version. Similar names do not establish equivalent areas. | Harmonize stable boundaries or specify how changed areas enter a longitudinal model. Assess dependence on between-region comparisons. | Region intercepts are survey-specific; they do not represent a single stable regional effect across surveys. |
| **Measurement timing and migration** | Annual exposures summarize the entry year. Household wealth, urban residence and education are measured at interview; location is also residence at interview. Historical residence is unknown. | Review temporal ordering and availability of residence histories. Consider whether survey-time variables are confounders, proxies or consequences of exposure/outcome. | Keep the specified entry-year annual assignment; use survey-time individual covariates provisionally. |
| **Calendar and death-age coding** | Older Ethiopian recodes use an approximate +92 CMC adjustment; ET8AFL is Gregorian. Month-based neonatal deaths include reported days 0–30, whereas Burstein's text uses a 28-day boundary. The build excluded 38 ambiguous death-band histories, 696 impossible death/interview timings and 1,810 invalid weights. | Verify calendar conversions, age-reporting/imputation flags, B6/B7 disagreements and sensitivity to neonatal/interval definitions. | Preserve provenance flags and current explicit rules; no fabricated exact death dates. |
| **Partial follow-up and earliest years** | The entry window includes 994,618 band opportunities that are incomplete by interview. Another 418,661 complete-band opportunities fall outside the configured 2000–2024 entry years. | Assess information loss and target-population consequences. Consider an interval-censored or appropriately timed person-time model that uses partial follow-up. | Apply the same full-potential-band eligibility rule to deaths and survivors. |

## Statistical specification and inference

| Issue | Work needed | Current trial treatment |
|---|---|---|
| **Final causal adjustment set** | Review a causal structure for malaria, HIV, development, health services and household conditions. Resolve treatment of WASH, wasting, vaccination, birth spacing and possible mediators. Assess nonlinear confounder relationships and interactions. | The current 18-variable regional/annual set is an agreed specification, not a validated sufficient adjustment set. WASH, DTP3/measles, delivery and short-birth-interval coverage are included; wasting is not. Regional birth-interval coverage excludes first births from its denominator without excluding them from mortality fitting. |
| **Survey weights and uncertainty** | Choose a weighting/target-population convention and a method accounting for sampling, mother/child dependence and PSU clustering. Validate strata definitions; UG61FL lacks strata for all 44,636 eligible rows. | Unweighted likelihood with survey and geographic random effects. These do not replace a survey-design variance estimator. Any reported intervals are approximate model-based intervals. |
| **Spline complexity, confounding and fit** | Check convergence, rank, age-specific support, smooth EDF/basis adequacy, calibration and concurvity between PfPR, time and development covariates. Refit suspect basis dimensions and assess geographic/temporal robustness. | Primary MAP fits use cr/k=5 for PfPR and cr/k=6 for time per band, estimated by discrete `mgcv::bam` with fREML and gamma=2. All selected complete cases enter the fit. |
| **Likelihood and rate interpretation** | Assess the constant within-band hazard assumption, dependence and residual heterogeneity; compare a logit or appropriate event-time specification if warranted. | Bernoulli band-death outcomes with cloglog link. The offset is the predetermined band width, not observed time to death. Hazard ratios and band-death probability ratios are distinct. |
| **Causal contrast and survivor selection** | Specify supported PfPR changes and target populations. A change from birth can alter who reaches later bands, so conditional age-band effects are not automatically total life-course effects. | A 40% to 20% PfPR contrast is evaluated within each age band, holding other predictors fixed. This remains an adjusted model association pending causal validation. |
| **Exposure uncertainty and extrapolation** | Assess MAP uncertainty, aggregation and support, and carry uncertainty through burden calculations. Document sensitivity decisions and their timing. | Primary fits use point-estimate regional MAP PfPR. Secondary burden estimates for 2005, 2015 and 2024 use the full MAP gamma=2 curves and national MAP prevalence. Exposure uncertainty is not propagated; support and transport assumptions remain explicit. |

## Pipeline work still needed

The [historical first completed fit](../results/cbh/age_band_complete_case_v1/REPORT.md) reported high older-band EDF and a small negative smoothing-Hessian eigenvalue. Those results describe that earlier joint fit, not the current seven MAP gamma=2 fits, which passed the recorded convergence, rank, covariance and positive-Hessian checks. Basis adequacy, confounding and uncertainty still require assessment. A repeated PfPR k-index across historical factor-by terms is a pooled diagnostic, not seven independent age-specific tests.

1. Migrate raw ZIP/registry ingestion, MAP extraction and external-data acquisition into the new pipeline. Stage 1 currently depends on local extraction snapshots, although it no longer sources legacy fitting scripts.
2. Resolve the source and mapping issues above, rebuild under explicit provenance, and compare included countries/years and mortality counts with the current snapshot.
3. Implement the agreed weighting/uncertainty method and planned sensitivity fits, followed by final plotting/reporting. Keep complete-case selection, model specification, cached fit and figure provenance aligned.

Supporting records: [dataset validation](../R_cbh/BUILD_VALIDATION.md), [HIV missingness](../R_cbh/HIV_MISSINGNESS.md), [geographic rules](../R_cbh/config/README.md), and [current primary specification](ANALYSIS_PLAN.md).
