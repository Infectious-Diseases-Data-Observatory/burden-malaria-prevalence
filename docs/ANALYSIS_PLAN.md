# Analysis plan: malaria prevalence and child mortality

Updated 18 September 2026 (17-variable contextual adjustment fitted as `primary_map_regional17_gamma2_v3`: sex, multiple birth and birth order removed; maternal age at each birth replaced by age at first birth; regional wasting and stunting prevalence added. The Snow prevalence comparison was removed from the plan the same day and its code archived. Main Figures 4–6 were combined into one three-panel Figure 4). This is a working analysis plan revised after inspection of exploratory results, not a prospective preregistration. The [previous plan](ANALYSIS_PLAN_BEFORE_MAP_GAMMA2.md) preserves the original audit, superseded specifications and decision history; the [code audit](CODE_AUDIT.md) compares each component with the current implementation and records the archival changes.

**Primary analysis:** seven separate child–age-band mortality models using **annual MAP PfPR₂–₁₀ at band entry**, fitted with **`mgcv::bam`, `gamma = 2`, PfPR `bs = "cr", k = 5`, and calendar-year `bs = "cr", k = 6`**. Use the full eligible MAP sample, including eligible observations after 2015. Each age band has its own time spline, confounder coefficients and survey/country/region random effects and variance parameters. Section 3.1 gives the complete specification. **The 18 September primary specification has 17 survey-region/national annual covariates (13 regional, four national): exclude sex composition, multiple births and birth order; use regional mean maternal age at first birth; add regional wasting and stunting prevalence. Retain urban residence, the other selected socioeconomic/health-service measures, and the existing UNICEF vaccination and within-survey regional-mean substitution rules. Section 2.4 defines the complete set.**

**Implementation status — fitted 18 September 2026:** the 17-variable specification is integrated into the model runner (`R_cbh/primary/specification.R`, default version `regional`) and fitted as **`primary_map_regional17_gamma2_v3`**. `Rscript run_all.R --primary` prepares the sample from the audited `planned17_audit` overlay, refits all seven bands (about a minute each), and regenerates effects, diagnostics, burden, figures and tables; the full run takes about ten minutes. All plan-defined primary figures and tables were regenerated and the verified assets exported to the Overleaf figure folders the same day. The 18-variable `primary_map_regional18_gamma2_v2` fits and figures are preserved as the previous benchmark; the [iteration report](../results/cbh/primary_map_regional17_gamma2_v3/REPORT.md) compares the two.

**Removed analysis (18 September 2026):** the comparison with the annual Snow prevalence estimates (matched MAP–Snow fits, Snow penalisation trials and Snow-based national mortality scenarios) is no longer part of this plan or the manuscript. Its code is in [`archive/2026-09-18-snow-comparison/`](../archive/2026-09-18-snow-comparison/README.md); its saved outputs are retained as historical records and are not linked from the results indexes. The [supplementary results index](<../Supplementary results/README.md>) links to the remaining sensitivity analyses.

**Current fitted version (17 variables):** the [primary manifest](../results/cbh/primary_map_regional17_gamma2_v3/fit_manifest.csv) records seven MAP gamma=2 fits using **5,465,305 child-band records, 1,686,004 children and 75,726 deaths in 916 survey-regions, 95 surveys and 34 countries**, matching the 18 September availability audit exactly. All seven passed convergence, rank, finite-covariance, positive smoothing-Hessian, exact fitted-input and compact-PfPR prediction checks; the 6–11 and 48–59 month bands took the tighter-tolerance restart under the same specification (recorded in `restarts.csv`). The [iteration comparison](../results/cbh/primary_map_regional17_gamma2_v3/REPORT.md) includes spline overlays, contrasts, sample accounting and national estimates for 2005/2015/2024 against the 18-variable fit.

**Previously fitted benchmarks:** preserve [primary_map_regional18_gamma2_v2](../results/cbh/primary_map_regional18_gamma2_v2/fit_manifest.csv) (18 regional/annual variables, 17 September) and [primary_map_gamma2_v1](../results/cbh/primary_map_gamma2_v1/fit_manifest.csv) (eleven-variable individual/country adjustment). Supplementary sensitivity fits remain from earlier iterations until separately refitted; do not relabel them as revised regional-adjustment results. See [current pipeline instructions](../R_cbh/primary/REGIONAL_REFIT.md).

## 1. Scientific questions and reporting choices

**Manuscript naming rule (code pointer):** call the proposed method **“PfPR-ACM model”** in every main-paper and supplementary figure that identifies the method, including legends, axis labels and captions. Do not use “New method”, “Our model” or “Primary MAP model” as its display name. Plotting code must source [R_cbh/reporting/labels.R](../R_cbh/reporting/labels.R) and use `cbh_paper_model_label()` rather than defining its own label. Where needed, append an exposure or sensitivity qualifier to this shared name. Preserve descriptive outcome labels and the no-title/no-subtitle convention; figures without a model-name label do not need an extra label. This is a presentation change, not a change to the model, estimates or internal data identifiers.

The primary causal question is: **if PfPR₂–₁₀ changes from X% to Y%, how does all-cause mortality in age group g change?** Estimate this separately for **<1, 1–5, 6–11, 12–23, 24–35, 36–47 and 48–59 completed months**. All seven age-specific effects, including neonatal mortality, are primary quantities of interest. Retain estimates regardless of direction or significance.

The exposure is *P. falciparum* prevalence standardized to ages 2–10 years, expressed on the 0–100 percentage scale. Regional annual prevalence is the same exposure definition for every age group. Values assigned to a child's successive bands can differ because their entry years differ. The outcome is death during a band, conditional on reaching it alive; the cloglog model estimates an annualized mortality hazard.

Report relative and absolute mortality changes for specified X%→Y% contrasts in the same target population. National malaria-attributable deaths are a **secondary extrapolation of the primary MAP model**. They additionally require assumptions about transportability, national exposure and the counterfactual intervention. Agreement with IHME, WHO or trials does not establish causal identification. The plan does not seek a null neonatal association or interpret one as proof of no confounding.

The age-band estimand conditions on reaching that band alive. An intervention beginning at birth can change who survives into later bands; conditional band effects are not automatically the total effect of a lifelong intervention. Keep this distinction explicit in causal interpretation and synthetic-cohort survival figures.

## 2. Stage 1 — make the analysis datasets

### 2.1 Inputs and provenance

The active builder is [R_cbh/01_make_analysis_data.R](../R_cbh/01_make_analysis_data.R), with [configuration](../R_cbh/00_config.R) and [dataset documentation](../R_cbh/README.md). It processes authorized local DHS Births Recodes and joins reviewed geography and external-data snapshots. Record source release, units, coverage, file hashes, survey versions and exclusions. Optional input retrieval is separate from local processing; do not refresh online sources because a cache is absent.

Keep every in-scope survey in the registry with a status for missing recodes, boundaries, dates, exposure or covariates. Survey-year MAP availability alone must not determine whether earlier band entries are usable. Configured entry years are 2000–2024.

### 2.2 Geography and exposure

Use survey/boundary-version-specific region keys, explicit normalization, curated synonyms and reviewed aggregation rules. Do not infer a match by elimination or accept an unreviewed fuzzy match. Keep unresolved records in the exclusion ledger. Current region identifiers include the survey/boundary version; they are not automatically longitudinal regional identities.

Assign regional annual MAP PfPR₂–₁₀ from the **calendar year containing band entry**. Hold this value fixed over the band, independently of death timing. The current analysis does not average prevalence over a child's realized person-time or the full band, interpolate between years, borrow the nearest year, or carry the last exposure forward. Preserve genuine zero prevalence and distinguish it from missing exposure. `calendar_year` is fractional entry time; `entry_year` is the integer source year.

The region is the mother's residence at interview; historical migration is not reconstructed. Record coverage and geographic-source flags. Consolidation and validation of the inherited DHS regional raster extracts remain necessary; the cell-area correction used in national burden extraction does not by itself validate the older regional extraction.

### 2.3 Child–age-band records and eligibility

Use one row per child and age band, with disjoint intervals `[0,1), [1,6), [6,12), [12,24), [24,36), [36,48), [48,60)` months. Predetermined band widths are 1/12, 5/12, 6/12, 1, 1, 1 and 1 year.

1. The child must reach the band alive; exclude bands after death.
2. Entry must satisfy `interview_cmc - 60 <= band_entry_cmc < interview_cmc`.
3. The full potential band must end by interview, **for deaths and survivors alike**. Exclude a death in a band that would otherwise be incomplete.
4. The entry year must lie within the configured exposure period. Apply geography, exposure and selected-covariate availability rules with recorded exclusion reasons.

`death` is 1 for death during the band and 0 for survival through it. `band_years` is the full predetermined width, **not observed time until death**. The earlier aggregate Poisson/negative-binomial person-time dataset is not the primary analysis dataset.

Retain recorded death-age units/days and B6/B7 disagreement flags. The current DHS month-based neonatal coding includes reported days 0–30; its boundary differs from the exact 28-day definition in the Burstein text and the IHME neonatal export. Review ambiguous intervals, impossible dates, duplicate identifiers, age heaping and calendar conversions. Older Ethiopian recodes use the declared +92 CMC approximation; ET8AFL is already Gregorian. Do not replace documented rules with generic calendar heuristics.

Validate synthetic birth histories, age boundaries, five-year eligibility, complete-band selection, annual joins and exclusion accounting. Preserve survey, mother, cluster and stratum identifiers under the ignored data directory for dependence/design checks. [Build validation](../R_cbh/BUILD_VALIDATION.md) and [open issues](OPEN_ANALYSIS_ISSUES.md) record current evidence and limitations.

### 2.4 Covariates, HIV imputation and missing data

**Decision, 18 September 2026:** select contextual predictors of mortality and higher PfPR at the subnational level rather than individual birth-composition predictors. All DHS-derived predictors are **survey-region summaries**, with no individual child, mother or household predictor in the primary model. Exclude sex composition, multiple births and birth order from both adjustment and complete-case requirements. Replace average maternal age at each birth with regional mean maternal age **at first birth**. Add regional wasting and stunting prevalence as two separate covariates. Retain urban residence, education, wealth, DTP3/measles, facility delivery, short birth interval, WASH and electricity. Hib3, PCV, rotavirus, exclusive breastfeeding and underweight remain excluded. The four existing national annual measures are retained where regional data are unavailable; they must not be described as subnational measurements. The outcome remains a child–age-band observation.

The intended role is adjustment for regional conditions associated with mortality and transmission, not selection by statistical significance or an established claim that every covariate causes higher PfPR. Nutritional prevalence is included by the present decision, but it may also reflect consequences of malaria. Survey-time anthropometry in surviving children cannot by itself establish that nutrition preceded the historical exposure or death. Record this possible mediation/selection issue and compare the primary model with a model omitting both nutritional measures on the same analysis sample. Regional aggregation does not establish a sufficient causal adjustment set.

| Confounder | Regional definition and extraction denominator |
|---|---|
| Maternal age at first birth | Weighted mean of DHS `v212` (completed years at first live birth) among distinct interviewed mothers with a live birth in the 60 months before interview; count each mother once. Her first birth need not have occurred within those 60 months. Do not use maternal age at each recent birth, current age, or restrict the denominator to recent first births. Proposed field: `mean_maternal_age_first_birth` |
| Maternal education; wealth; urban residence | Weighted mean education years, mean wealth-quintile score (1–5), and percentage urban among distinct interviewed mothers with a birth in those 60 months; each mother counted once. These are summaries of mothers represented in the birth history, not all households or all women |
| DTP3; measles vaccination | Published DHS survey-region coverage (normally ages 12–23 months); if missing, substitute the UNICEF WUENIC national estimate for the survey year (`IM_DTP3`, `IM_MCV1`). Preserve regional estimates and mark national substitutions |
| Facility delivery | Published regional percentage delivered in a health facility; prefer the five-year recall window, otherwise retain the reported three-/two-year window with its flag |
| Birth interval | Regional percentage of non-first births in the previous five years with a preceding interval <24 months: sum published 7–17- and 18–23-month categories. First births are outside this coverage denominator; they are **not excluded from mortality analysis** |
| WASH | Two separate published regional percentages: households with an improved drinking-water source and households with an improved sanitation facility |
| Electricity | Published regional percentage of households with electricity |
| Wasting | Survey-region percentage of measured children aged 0–59 months with weight-for-height z-score below −2 using WHO Child Growth Standards, including severe wasting; valid measurements only. Prefer the published DHS regional indicator with its source denominator and weights. Proposed field: `wasting_pct` |
| Stunting | Survey-region percentage of measured children aged 0–59 months with height-for-age z-score below −2 using WHO Child Growth Standards, including severe stunting; valid measurements only. Prefer the published DHS regional indicator with its source denominator and weights. Proposed field: `stunting_pct` |
| HIV incidence, GDP, health expenditure, political stability | Existing national annual measures assigned to each survey-region at band-entry year; log-transform the first three as previously specified |

For recode-derived means use DHS women's sampling weights (`v005/1e6`) and the denominator above, calculated **before exposure/outcome complete-case selection**. Never average child-band rows, which would weight surviving children repeatedly. Published household indicators use their source's household denominator and weighting; do not approximate these using women’s weights on deduplicated birth histories. For wasting/stunting, preserve the survey's anthropometry population, age coverage, reference standard and weighting; do not use birth-history rows as the denominator or require a mortality child's own anthropometric measurement. Audit availability and measurement flags before adopting a published estimate; do not silently combine older growth references or narrower age ranges with WHO-standard 0–59-month estimates. If reconstruction is needed, use the appropriate anthropometry recode, valid WHO z-scores and its sampling weights. Missing measurements must not be treated as absence of malnutrition. Preserve the region key's survey/boundary version. National annual variables remain constant across regions within a country-year because no regional source is available; assigning them to a region does not create regional variation.

All 17 scalar covariates (13 regional and four national) enter linearly after centering/scaling on the revised complete-case modelling sample. Mean wealth-quintile score is a numerical regional summary, not the former five-level individual factor. Save new scaling in a new analysis version; do not reuse the old individual-covariate scaling. Within each separately fitted age band, regional covariates have their own coefficients. PfPR and fractional calendar time remain band-entry exposure/time variables rather than being averaged over children.

Survey-region DHS coverage, wealth, urban residence and education describe survey-time conditions or the indicator's stated retrospective window. Do not relabel them as reconstructed historical values at band entry. **Urban/rural residence is required as `urban_pct`, the weighted percentage urban among distinct interviewed mothers with a recent birth in the region.** DTP3 and measles are the only selected vaccine coverage measures. Preserve the excluded vaccine/breastfeeding extraction history, but neither their missingness nor pre-introduction assumptions affect primary eligibility. Maternal death fraction and hypothetical children remain outside the adjustment set. Wasting and stunting are now required regional predictors; their new missingness must be audited before refitting.

**National UNICEF vaccination imputation:** retain every available regional DTP3/measles value; fill only unavailable values with a valid national WUENIC percentage for the **same survey year**. This timing matches those survey-time regional measures. Preserve genuine reported zeros. This is deterministic national substitution, not a fitted imputation model. The historical national Hib3/PCV/rotavirus series and pre-rollout zero scenarios are retained for provenance only and are not used by the current primary adjustment set.

Save the value before substitution, an imputation indicator, national source year and source category for every vaccine. Substitutions remain national values assigned to survey-regions; they do not reconstruct within-country differences. Imputation flags describe provenance and do not automatically add predictor terms. Uncertainty in national estimates or regional departures from them is not propagated by this single substitution. Include an observed-regional-only/reduced-adjustment sensitivity to assess dependence on the fallback. Remaining regional gaps follow the available-region rule below; retained national annual gaps remain missing after the existing HIV imputation.

**Regional missingness fallback:** after the DTP3/measles national fallback, substitute each missing regional covariate with the arithmetic mean of finite available regions in the **same survey**, separately for each variable. Count each donor region once, not once per child-band record. Preserve all observed values and store original values, imputation flags, donor counts and source labels. Missing individual responses are already excluded from within-region weighted means and must not propagate into an otherwise estimable aggregate. If no usable donor region exists in that survey, leave the value missing; do not borrow across surveys or apply regional means to national country-year variables. This carries forward the user-specified within-survey imputation approach; its uncertainty is not propagated. Retain a no-regional-substitution sensitivity.

**Imputation of the remaining covariate gaps (sensitivity version, decided 18 September 2026):** after the two fallbacks above, every remaining gap is structural rather than scattered: whole-survey gaps in published regional indicators and country-year gaps in national series, together excluding 892,497 MAP-eligible records (14%) and 25 whole surveys, including all five MIS surveys. These gaps are imputed in a **separate sensitivity version**, `primary_map_regional17_imputed_gamma2_v4`, which keeps every MAP-eligible record; the complete-case version remains the primary analysis until the comparison has been reviewed. Imputed values never replace observed ones.

| Gap | Structure | Imputation |
|---|---|---|
| Wasting, stunting, facility delivery, electricity, wealth-quintile score | Whole-survey gaps (wasting 10 surveys, stunting 9, facility delivery 5, electricity 1, wealth 9 surveys from 2000–2001 without a wealth index) | Chained-equation multiple imputation at the survey-region level ([covariates/16_impute_regional.R](../R_cbh/covariates/16_impute_regional.R)): `mice`, predictive mean matching, m = 10, maxit = 20, fixed seed, on the 1,126 survey-regions of the current overlay. Targets: the 13 regional covariates and regional MAP PfPR at survey year; predictors: all targets plus survey year, a country factor and the national series at survey year. Whole-survey gaps borrow through the country factor and the covariate relationships; matching donates observed values, so imputations stay in range. Point overlay = mean of the 10 imputations; all 10 retained |
| Political stability | Entry year 2001 for every country (no WGI round) | Mean of the country's 2000 and 2002 estimates ([covariates/15_impute_national.R](../R_cbh/covariates/15_impute_national.R)); deterministic, flagged |
| Health expenditure per capita | Zimbabwe 2000–2009; every country's 2024 | GAM on the observed log panel (`s(year) + log GDP + country random intercept and slope`, REML); point = fitted mean, 10 coefficient-posterior draws for propagation; flags distinguish the within-series fill from the one-year extrapolation |
| Child HIV incidence | Liberia and São Tomé and Príncipe have no UNAIDS adolescent or child incidence series | Extended incidence model ([hiv/01_fit_incidence.R --extended](../R_cbh/hiv/01_fit_incidence.R)): the adolescent path is a latent AR(1) variable, so the child series is predicted through the regional and temporal structure with prior-distribution country effects; status `imputed_no_adolescent_series`. Written to a separate extended panel so the frozen base panel of the primary is untouched; validated by treating two observed West and Central Africa countries' adolescent series as latent |

Including PfPR as an auxiliary variable follows the usual multiple-imputation advice to include the analysis model's variables; the imputed quantities are adjustment covariates, not the exposure or outcome. The point fit uses one imputed dataset and does not reflect imputation uncertainty; the separate multiple-imputation check ([sensitivity/imputation/01_propagate.R](../R_cbh/sensitivity/imputation/01_propagate.R)) refits all seven bands in each of the 10 imputed datasets with smoothing parameters fixed at the point fit and pools the PfPR log hazard ratios with Rubin's rules. GDP has no gaps. Imputation audits: [regional](../results/cbh/covariate_imputation_v4/REGIONAL_IMPUTATION.md), [national](../results/cbh/covariate_imputation_v4/NATIONAL_IMPUTATION.md). Section 3.4.3 describes the fitted version.

The [regional pipeline](../R_cbh/covariates/README.md) separately retrieves public indicator snapshots, extracts weighted recode summaries, resolves published regional joins and audits availability. Scripts 01–10 produce the 18-variable overlay; scripts 11–13 add the first-birth-age extraction (`v212`), the published WHO-standard wasting/stunting indicators (`CN_NUTS_C_WH2`, `CN_NUTS_C_HA2`) and the 17-variable availability audit, writing the `planned17_audit` overlay that model preparation consumes. The available-region fallback is applied to the new regional measures after their observed summaries are defined and validated; whole-survey gaps remain missing. Exposure/region identity must still be resolved; a missing regional vaccination covariate may use the declared UNICEF fallback, without imputing location or PfPR. The [missingness audit](../results/cbh/regional_adjustment_reduced_v3/README.md) distinguishes missing records, entirely lost survey-regions and partially reduced survey-regions. A regional mean can be observed when some individual responses are missing; respondent-level missingness and small denominators are reported separately. Do not drop a mortality record solely because that child's own confounder response is missing.

Use **child HIV incidence at ages 0–14 per 1,000 uninfected population**, not HIV prevalence or infection counts. The [HIV imputation model](../R_cbh/hiv/README.md) uses child and adolescent incidence jointly, calendar-year trends, geographic/country effects and within-country temporal correlation. Preserve reported child rates, handle censored values as censored, and predict missing child series. Adolescent incidence informs imputation; it is not a separate predictor in the mortality model.

The revised primary model retains **one fixed posterior-median child-incidence imputation** and requires complete cases for the selected **regional/annual** model variables **after UNICEF vaccination and available-region substitution**. Keep censoring, imputation and missing-source flags; unsupported country-year values remain missing. The historical HIV prevalence field in base shards is provenance only. There is no country/overall-median imputation. The only new national substitution is the explicitly specified UNICEF vaccination fallback.

Report missingness and exclusions by country, survey, year, age and exposure. Missing HIV/source data, unresolved geography and complete-case selection remain substantive issues. Retain posterior HIV trajectories for future uncertainty propagation through all seven primary models. For validation with held-out surveys/countries, estimate preprocessing and imputation within training folds where appropriate.

**Availability under the previous 18-variable policy, 17 September:** start with 6,357,802 MAP-eligible child-band records across 120 surveys. Complete cases after the declared substitutions retain **5,680,117 records, 1,755,838 children and 78,634 deaths in 973 survey-regions, 100 surveys and 34 countries**; 10.66% of the starting records remain excluded. Relative to the previous fitted sample, 973/1,015 survey-regions remain and **42 (4.14%)** are lost; 5,617,520 of its 5,885,022 records remain (4.55% excluded). Urban proportion has 0% missingness in every audited denominator. See the [current audit](../results/cbh/regional_adjustment_reduced_v3/README.md). The [22-variable UNICEF audit](../results/cbh/regional_adjustment_unicef_v2/README.md) and its pre-rollout/region-mean scenarios remain historical and must not supply current sample counts. These availability counts match the 17 September fitted sample only.

**Availability under the current 17-variable policy, 18 September:** the [new covariate table and audit](../results/cbh/planned17_covariate_missingness/README.md) starts from the same 6,357,802 MAP-eligible child-band records in 1,113 survey-regions and 120 surveys. After the agreed HIV, UNICEF vaccination and available-region substitutions, complete cases retain **5,465,305 records, 1,686,004 children and 75,726 deaths in 916 survey-regions, 95 surveys and 34 countries**. Regional age at first birth is available throughout; wasting and stunting remain missing for 6.83% and 5.68% of eligible records respectively. Relative to the 18-variable version, 214,812 records and 57 survey-regions from five surveys are additionally lost, with no records recovered. The fitted `primary_map_regional17_gamma2_v3` sample reproduces these counts exactly; preparation stops if it does not. [Variable-level before/after missingness](../results/cbh/planned17_covariate_missingness/covariate_missingness.csv) distinguishes missing assigned regional/country-year values from individual nonresponse. The separate extraction/audit scripts `covariates/11_extract_first_birth_age.R`, `12_fetch_nutrition.py` and `13_audit_planned17.R` preserve current model inputs and outputs.

### 2.5 Analysis-ready outputs

The 17-variable overlay is saved in `data/derived_cbh/regional_adjustment/planned17_audit/`, with the missingness audit under `results/cbh/planned17_covariate_missingness/`. The current modelling sample, scaling, executable formula and fit manifest are in the `primary_map_regional17_gamma2_v3` paths below. Do not overwrite the 18-variable benchmark or reuse its counts/scaling as if they described the revised sample.

| Output | Role |
|---|---|
| `data/derived_cbh/manifest.rds` and `child_bands/<survey>.rds` | Authoritative build manifest and eligible child-band shards with audits |
| `data/derived_cbh/hiv_incidence/child_incidence_country_year.csv` | Incidence estimates and source/imputation flags |
| `data/derived_cbh/regional_adjustment/planned17_audit/regional_covariates_wide.csv` | Current 13-regional-variable overlay after UNICEF and available-region fallback, including original values and imputation flags |
| `data/derived_cbh/regional_adjustment/reduced_v3/regional_covariates_wide.csv` | Previous 14-regional-variable overlay (18-variable policy); benchmark only |
| `data/derived_cbh/regional_adjustment/unicef/country_year_estimates.csv` | WUENIC source snapshot: only DTP3/measles are selected for current primary adjustment; the other three vaccine series are historical |
| `results/cbh/planned17_covariate_missingness/complete_case_summary.csv` | Current availability after substitution, including losses relative to the 18-variable sample |
| `data/derived_cbh/models/age_band_hiv_incidence_shared_time_v3/complete_case_dataset.rds` | Previous individual-adjustment modelling sample and preprocessing; benchmark only |
| `results/cbh/primary_map_gamma2_v1/fit_manifest.csv` | Seven previous-adjustment MAP gamma=2 objects; all entries are `map_full` |
| `data/derived_cbh/models/primary_map_regional17_gamma2_v3/complete_case_dataset.rds` | Current prepared sample, 13 regional predictors, four annual covariates and their scaling |
| `results/cbh/primary_map_regional17_gamma2_v3/fit_manifest.csv` | Seven current 17-variable MAP gamma=2 models |
| `data/derived_cbh/models/primary_map_regional18_gamma2_v2/`, `results/cbh/primary_map_regional18_gamma2_v2/` | Previous 18-variable sample and fits; benchmark only |
| `data/derived_cbh/regional_adjustment/imputed_v4/regional_covariates_wide.csv`, `regional_imputations.rds`, `national_covariates_imputed.csv`, `national_imputations.rds` | Imputed regional overlay (point and 10 imputations) and imputed national panel for the sensitivity version |
| `data/derived_cbh/hiv_incidence/child_incidence_country_year_extended.csv`, `child_incidence_draws_extended.rds` | Extended HIV incidence panel including Liberia and São Tomé; used only by the imputed version |
| `data/derived_cbh/models/primary_map_regional17_imputed_gamma2_v4/complete_case_dataset.rds`, `results/cbh/primary_map_regional17_imputed_gamma2_v4/fit_manifest.csv` | Imputed-covariate sensitivity sample (all MAP-eligible records) and its seven fits |
| Aggregate selection, missingness, geography and model diagnostics | Inspectable accounting without microdata |

The **previously fitted** sample contains **1,817,912 distinct children contributing 5,885,022 child-band records and 82,415 deaths from 105 surveys in 34 countries**. The [15 September verification](../results/cbh/code_audit_2026_09_15/primary_fit_checks.csv) compared every primary fitted outcome, predictor and offset with the saved prepared sample and verified the selected model hashes and knots. Keep raw/derived microdata and fitted objects under ignored `data/`; export only approved aggregates to results. Validate unique child-band keys and input hashes. The primary-only fitter now reads this prepared dataset directly and checks every outcome, predictor and offset. Its reference MAP knots are preserved in a committed snapshot. A future consolidated data stage must still operate independently of existing outcome models and results tables.

## 3. Stage 2 — primary and sensitivity analyses

### 3.1 Primary working model

**Separate age-band MAP models with gamma=2, with the regional adjustment revision of 18 September 2026.** This is the fitted specification of `primary_map_regional17_gamma2_v3`; `results/cbh/primary_map_regional17_gamma2_v3/model_formula.txt` records the formula as fitted. For each of the seven bands, fit:

```r
form <- death ~
  s(pfpr_pct, bs = "cr", k = 5) +
  s(calendar_year, bs = "cr", k = 6) +
  z_mean_maternal_age_first_birth + z_mean_maternal_education_years +
  z_mean_wealth_quintile + z_urban_pct +
  z_dtp3_pct + z_measles_pct + z_facility_delivery_pct +
  z_short_birth_interval_pct + z_improved_water_pct +
  z_improved_sanitation_pct + z_electricity_pct +
  z_wasting_pct + z_stunting_pct +
  z_log_hiv_incidence + z_log_gdp_pc +
  z_log_health_expenditure_pc + z_political_stability +
  s(survey, bs = "re") + s(country, bs = "re") +
  s(region, bs = "re") + offset(log(band_years))

# d_g: revised regional-adjustment complete cases for this one age band.
# knots_g: saved PfPR and calendar-year knots from the MAP reference fit.
fit_g <- mgcv::bam(
  form, data = d_g, knots = knots_g,
  family = binomial(link = "cloglog"),
  method = "fREML", discrete = TRUE,
  gamma = 2, select = FALSE,
  na.action = na.fail, control = mgcv::gam.control(maxit = 100)
)
```

For child i and band g, with width Δg in years:

\[
D_{ig}\sim\operatorname{Bernoulli}(q_{ig}),\qquad
q_{ig}=1-\exp(-\Delta_g\lambda_{ig}),
\]

\[
\log\lambda_{ig}=\alpha_g+f_g(P_{r(i),y_{ig}})+h_g(t_{ig})+
\mathbf X_{r(i),s(i),y_{ig}}^{\mathsf T}\boldsymbol\beta_g+
u_{s(i),g}+v_{c(i),g}+b_{r(i),g}.
\]

Here X contains survey-region summaries from Section 2.4 and national annual covariates assigned to those regions; it contains no individual confounding predictor. P is regional annual MAP PfPR₂–₁₀, y is band-entry year, and t is fractional calendar year at entry. The exposure surface has no age-band-specific definition; the function f and the entry year depend on the band. Equivalently, `cloglog(q_ig) = log(Δg) + log(λ_ig)`.

Each fit estimates its own intercept, PfPR and time curves, confounder coefficients and survey/country/region random intercepts. Within each model, the random effects are independent zero-mean Gaussians, with a separate estimated variance for each grouping type. No time spline, random intercept or variance parameter is shared across age bands. Separate fitting does not imply independent sampling errors across ages because children and surveys contribute to multiple bands.

`gamma = 2` changes smoothing selection throughout the model, including time and random effects; it is not a PfPR-only multiplier. The primary PfPR basis remains **cr**, not cs. No monotonicity constraint is imposed. Reuse the reference MAP knots for these promoted fits; they are recorded and verified in the fitting pipeline.

The unweighted likelihood, fixed HIV imputation and conditional model covariance are current working choices. Address survey design, residual dependence and imputation/exposure uncertainty before claiming comprehensive uncertainty. The primary decision follows review of results and user instruction; it does not establish superior causal identification or formal model-selection evidence.

**Executable formula/data specification (17 variables):** [primary/specification.R](../R_cbh/primary/specification.R) defines `cbh_primary_regional_spec()` and `cbh_primary_regional_formula()`, which return the 13 regional and four annual covariates above when `settings$nutrition` is true; [primary/settings.R](../R_cbh/primary/settings.R) maps version `regional` (the default for the runner and reporting) to `primary_map_regional17_gamma2_v3` and `regional18` to the preserved 18-variable version. [covariates/regional.R](../R_cbh/covariates/regional.R) keeps the older `cbh_regional_spec()` (18 variables) for the benchmark and the `cbh_regional_mean_fill()` fallback; [covariates/unicef.R](../R_cbh/covariates/unicef.R) implements national vaccination substitution. The [regional runner](../R_cbh/primary/run_regional.R) assembles the `planned17_audit` overlay and fixed HIV incidence into a versioned prepared dataset (`00_prepare_regional.R`, which stops unless the counts reproduce the availability audit), refits all seven models (`01_fit.R`), and calculates effects, diagnostics, comparisons, burden and figures. Shared stages select this version with `CBH_PRIMARY_VERSION=regional`; the committed MAP knots are unchanged. The fits are in `data/derived_cbh/models/primary_map_regional17_gamma2_v3/`. Comparison with the previous iteration combines the changed adjustment set and the changed complete-case sample; it is not an isolated test of any one covariate. **Previous benchmark implementation:** [primary/01_fit.R](../R_cbh/primary/01_fit.R), configured in [settings.R](../R_cbh/primary/settings.R), with `map_full_age_1` through `map_full_age_7` in the [primary manifest](../results/cbh/primary_map_gamma2_v1/fit_manifest.csv). Fitted objects are in `data/derived_cbh/models/primary_map_gamma2_v1/`. The fitting sample is read from the existing prepared dataset, with saved scaling and [reference knots](../R_cbh/primary/reference_knots.csv). The original `map_full` fits in `results/cbh/map_snow_gamma2_v1/` remain the legacy numerical reference for this rerun; the workflow that produced them is archived and the directory name is historical. The older `sensitivity_single_imputation/age_*.rds` fits used gamma=1.

### 3.2 Effect summaries

For each of the **seven** age groups and any specified X%→Y% contrast:

\[
HR_g(X\to Y)=\exp\{f_g(Y)-f_g(X)\},\qquad
\%\Delta_g=100[HR_g(X\to Y)-1].
\]

Absolute changes require an explicit target population or calibrated baseline: `rate_g(Y) - rate_g(X)`. Express rates with declared person-year units. For band death probabilities use `q = 1 - exp(-Δg × rate)`; a hazard ratio is not a death-probability ratio. Random effects and other additive predictors cancel in a within-target hazard ratio, but not in marginal absolute-rate standardization.

Report full curves, 40%→20% contrasts and exposure support; additional AF anchors at 10%, 30% and 50% remain useful. For reference p0, `AF_g(p,p0) = 1 - exp[f_g(p0)-f_g(p)]`. Keep the zero reference explicit, assess a 1% reference as a sensitivity, and flag extrapolation. Retain signed effects and intervals, including negative values. The current CBH burden calculation does not truncate negative contributions.

Age-band intervals currently condition on smoothing parameters, one HIV imputation and point-estimate exposure. Do not sum marginal confidence limits or assume independent age-band errors to construct national intervals. Future uncertainty propagation should reuse common survey/design replicates and HIV trajectories across bands and countries before aggregating.

### 3.3 Diagnostics and validation

Check convergence, coefficient rank, finite covariance, smoothing gradients/Hessian, fitted outcomes, residual patterns, exposure support and influential surveys/countries. Verify every stored outcome, predictor and offset against intended inputs. Check compact spline contrasts/variances against full prediction matrices, including zero-PfPR extrapolation. Keep exact model hashes, sample definitions, knots, software versions and warning records.

Numerically flagged fits may receive a tighter-tolerance restart under the **same gamma and specification**; record original and selected attempts. Do not use fREML values to rank models across different gamma values or exposure sources. All seven currently selected MAP gamma=2 fits passed the recorded numerical checks; in the 17-variable version the 6–11 and 48–59 month fits had a negative smoothing-Hessian eigenvalue at the default tolerance and their restarts were selected. The primary fitter accepts a restart only if it also reduces the maximum smoothing gradient; the subgroup refits of Section 3.4.1 drop that gradient condition (the gradients are already of order 1e-5 and can move either way) and accept a restart that converges with full rank, finite covariance, a positive smoothing Hessian and a fREML criterion no worse than the original, recording both gradients in `restarts.csv`.

Use survey-grouped cross-validation for flexible-structure comparisons and country-held-out validation for transportability. Keep all records from each survey together. Examine within-region versus between-region exposure information; the existing coding does not itself identify a within-region causal effect. Assess smoothing-uncertainty-corrected covariance when available and record when it is unavailable. Bayesian sampling checks apply only if a Bayesian extension is undertaken, not as requirements for the selected bam fit.

### 3.4 Supplementary and sensitivity analyses

Each sensitivity needs a declared change, sample, exposure source, gamma, formula, diagnostics and uncertainty method. Report the primary model on the corresponding restricted sample when necessary. Keep specification choices independent of effect direction or significance; do not automatically cross every sensitivity with every other one.

| Priority / role | Comparison | Status and specification |
|---|---|---|
| **Required key sensitivity** | Subgroup refits: Sahel; Eastern Africa; surveys before/after the median survey year. Joint versus separate-age structure | **Completed 18 September 2026** on the 17-variable sample at gamma=2 (`results/cbh/subgroups_map_gamma2_v1/`, supplementary overlay figure). Joint versus separate remains planned. Earlier gamma=1 exploratory results remain historical. Section 3.4.1. |
| Supplementary: smoothing | Primary MAP gamma=2 versus MAP gamma=1 | Existing fits/results retained. |
| Required: eligibility and timing | Shorter lookback; entry-year lag; complete-band versus appropriate partial-follow-up alternatives | Adapt to current child-band records; no silent annual borrowing or return to the superseded window-count likelihood. |
| Required: age coding | Recorded days, month/year heaping, B6/B7 disagreement and alternative age boundaries | Primary has seven DHS completed-month bands; the historical 3-versus-4-month split is not the primary definition. |
| Required: missingness and adjustment | Selection patterns, HIV censoring/absent series, imputation uncertainty and reduced adjustment blocks | The 17-variable regional/annual set in Section 2.4 is primary. **Imputed-covariate version** `primary_map_regional17_imputed_gamma2_v4` (Section 3.4.3) refits the same specification with every remaining covariate gap imputed, retaining all MAP-eligible records, plus a multiple-imputation check. Compare selection and effects with the saved 18-variable regional and earlier individual-adjustment benchmarks. A same-sample comparison omitting both wasting and stunting remains planned. |
| Required: design and dependence | Survey weights; common cluster/stratum bootstrap; residual regional and within-child dependence | Retain unweighted primary point fit as reference; do not substitute rounded effective counts without a separate model definition. |
| Required: dose response / reference | Basis dimensions, linear comparison, 0% versus 1%, exposure restrictions | Match samples; report all supported contrasts and low-PfPR sensitivity. |
| Secondary: heterogeneity | Country omission, calendar interaction, within/between exposure and covariate timing | Separate, declared extensions of the primary structure. |
| Secondary: burden assumptions | Geographic coverage, population weights, national-mean versus subnational integration, age allocation and denominator sources | IHME all-cause is current baseline; IGME/WHO comparisons are supplementary alternatives. |
| Optional independent checks | Measured parasitaemia, fieldwork season, trial triangulation and alternative mortality estimators | Retain only for a stated scientific question; historical survey-region/NB/brms work remains archived. |

### 3.4.1 Key PfPR spline sensitivity analysis — current seven-band CBH model

<a id="key-pfpr-spline-sensitivity-analysis-current-seven-band-cbh-model"></a>

Use the seven 17-variable MAP gamma=2 models in `primary_map_regional17_gamma2_v3` as the primary reference. The saved `primary_map_regional18_gamma2_v2` models remain a previous-specification benchmark. Hold the median child HIV imputation, covariate selection/scaling and entry-year exposure timing fixed. Re-estimate smoothing parameters in each new fit at **gamma=2**, retaining the basis types and dimensions; record subgroup-specific knots and support.

**Subgroup refits (decided and completed 18 September 2026):** [R_cbh/sensitivity/subgroups/01_fit.R](../R_cbh/sensitivity/subgroups/01_fit.R) subsets the saved 17-variable prepared sample and refits all seven age-band models in each of four subsets, 28 fits in total, with subgroup-specific knots at quantiles of distinct predictor values, the full-sample covariate scaling, and the same formula, gamma, likelihood and offset as the primary. Whole survey regions enter; the period split counts each survey once. [02_report.R](../R_cbh/sensitivity/subgroups/02_report.R) overlays `f_g(P)-f_g(20)` for the full analysis and every subset on common axes, each over its own central 95% exposure range, and tabulates 40%→20% and 20%→0% contrasts. The overlay is a **supplementary figure of the manuscript** (`results/cbh/subgroups_map_gamma2_v1/sfig_pfpr_splines_by_subgroup.png`, exported to `Supplementary Figures/`). Subgroup and full-sample fits overlap, so compare descriptively; do not treat interval overlap as an interaction test.

- **Seasonal-area restriction (Sahel, 12°N):** survey regions with a cached boundary centroid at or north of 12°N, west of 36°E and outside Ethiopia, Eritrea, Somalia and Djibouti. This operational geographic proxy selects whole regions, not child locations or measured seasonality; Nigeria's broad northern-zone centroids fall below 12°N and are excluded. On the 17-variable sample: **349,764 children, 1,136,321 records and 14,545 deaths in 182 survey regions, 23 surveys and seven countries** (Burkina Faso, Chad, The Gambia, Mali, Mauritania, Niger and Senegal). The earlier Sahel refit on the 11-variable fit ([results](../results/cbh/sahel_map_gamma2_v1/REPORT.md)) is superseded. This is distinct from the descriptive monthly-mortality analysis using an 11°N cutoff.
- **Eastern Africa:** UN M49 Eastern Africa countries only (in the sample: Burundi, Comoros, Ethiopia, Kenya, Madagascar, Malawi, Mozambique, Rwanda, Tanzania, Uganda, Zambia, Zimbabwe): **641,126 children, 2,063,089 records and 23,583 deaths in 372 survey regions, 38 surveys and 12 countries**. This replaces the earlier plan for a West versus East-plus-Central split; a Western Africa refit can be added with the same code if wanted.
- **Survey period:** surveys up to the median survey year versus after it, ties to the early group. Median among the 95 included surveys: **2013**; early = 50 surveys (2003–2013), **782,384 children, 2,427,154 records, 42,210 deaths, 411 survey regions, 29 countries**; late = 45 surveys (2014–2024), **903,620 children, 3,038,151 records, 33,516 deaths, 505 survey regions, 28 countries**. Exposure years overlap between periods because of the five-year lookback.
- **Joint versus separate:** compare with a joint model having age-specific PfPR curves and confounder coefficients but one shared calendar-year spline and the historical joint random-effect structure. The completed joint/separate comparison used gamma=1; a matched gamma=2 joint fit remains to be run. Do not relabel the old comparison as gamma=2.

The earlier [key figure](<../Key results/pfpr_spline_sensitivity_by_age_geography_period.png>) and [report](../results/cbh/age_band_hiv_incidence_shared_time_v3/sensitivity_single_imputation/REPORT.md) retain their original gamma=1, joint-reference labels and are historical.

### 3.4.3 Imputed-covariate sensitivity version

`Rscript R_cbh/primary/run_regional.R --imputed` prepares, fits and reports `primary_map_regional17_imputed_gamma2_v4`: the 17-variable specification of Section 3.1 fitted to **every MAP-eligible child-band record** (6,357,802 records, 120 surveys, 36 countries, 1,113 survey-regions) after the imputations of Section 2.4, with confounders re-scaled on that sample, the same reference knots, gamma=2 and diagnostics. [00_prepare_regional.R](../R_cbh/primary/00_prepare_regional.R) reads the imputed regional overlay, the imputed national panel and the extended HIV panel, verifies their provenance hashes, and stops unless the retained sample equals the MAP-eligible total; it records how many records carry each kind of imputed value. The runner executes only the model stages (prepare, fit, effects, diagnostics, comparison, report); the study flow, survey map, paper manifest, results index and validation continue to describe the complete-case primary. [05_compare_regional.R](../R_cbh/primary/05_compare_regional.R) compares the curves, 40%→20% and 20%→0% contrasts and national estimates with `primary_map_regional17_gamma2_v3`, labelled "Complete-case covariates (v3)" and "Imputed covariates (v4)". The multiple-imputation check writes pooled curves and contrasts, the between-imputation share of variance and an overlay against the point fit under `multiple_imputation/`. Differences between v4 and v3 combine the added records (25 whole surveys among them) and the imputed values; they are not a test of either alone.

**Results (fitted 18–19 September 2026):** the imputed sample contains **6,357,802 records, 1,932,325 children and 90,938 deaths in 1,113 survey-regions, 120 surveys and 36 countries**, against 5,465,305 / 1,686,004 / 75,726 / 916 / 95 / 34 for the complete-case primary. Of the records, 558,906 carry at least one model-imputed regional covariate, 179,937 an interpolated 2001 political-stability value, 63,255 a modelled health-expenditure value and 142,323 an HIV incidence imputed without an adolescent series. All seven fits passed the numerical checks without restarts; the 36–47 and 48–59 month fits needed far more smoothing iterations than usual (33 and 111 minutes against about 80 seconds). The PfPR effects are close to the complete-case fit: 40%→20% hazard ratios of 0.947 / 0.918 / 0.815 / 0.854 / 0.854 / 0.847 / 0.981 across the seven bands (complete case: 0.936 / 0.924 / 0.814 / 0.835 / 0.824 / 0.839 / 0.929), and national attributable deaths of **936,871 in 2005, 653,313 in 2015 and 556,345 in 2024**, 3.8%, 1.8% and 1.6% below the primary. The extended HIV model passed its diagnostics (R-hat 1.007, no divergences); shared country-years differ from the frozen base panel by a median 0.064 on the log scale, and the latent-adolescent validation on Benin and Burkina Faso covered all observed child rates (median absolute log error 0.35). Results: [report](../results/cbh/primary_map_regional17_imputed_gamma2_v4/REPORT.md), [MI check](../results/cbh/primary_map_regional17_imputed_gamma2_v4/multiple_imputation/REPORT.md), [HIV validation](../results/cbh/hiv_incidence/latent_adolescent_validation.csv). The multiple-imputation check (completed 19 September 2026; ten imputed datasets, smoothing parameters fixed at the point fit, Rubin pooling with Barnard–Rubin degrees of freedom) reproduces the point-imputation hazard ratios to the third decimal in every band and contrast; the between-imputation share of total variance is at most 1.8% (1–5 months, 40%→20%) and below 1% elsewhere, so covariate-imputation uncertainty adds little to the conditional intervals ([pooled contrasts](../results/cbh/primary_map_regional17_imputed_gamma2_v4/multiple_imputation/pooled_contrasts.csv)). The supplementary manuscript figure of the two curve sets is produced by [02_supplementary_figure.R](../R_cbh/sensitivity/imputation/02_supplementary_figure.R) (Section 4). Conclusion for now: complete-case selection does not materially change the age-specific PfPR effects or the national totals, so the complete-case fit remains primary; the imputed version is reported as a sensitivity.

### 3.5 Secondary national burden from the primary MAP model

For **2005, 2015 and 2024**, use the full-sample MAP gamma=2 curves, annual population-weighted national MAP PfPR and the finer-age **IHME all-cause deaths/rates** export dated 9 September 2026. National population weights use GPW 2020 density × cell area and fractional country overlap. Keep the fixed population-weight year, partial coverage and absent exposure explicit; covered-area prevalence applied nationally remains an assumption. Do not fabricate zero prevalence for missing countries.

For country c and band g:

\[
HR_{cg}^{0}=\exp\{f_g(0)-f_g(P_{c,y})\},\quad
m_{cg}^{0}=m_{cg}^{IHME}HR_{cg}^{0},\quad
m_{cg}^{attr}=m_{cg}^{IHME}(1-HR_{cg}^{0}).
\]

Apply the same fraction to IHME annual death counts, holding annual person-time fixed. These are calibrated reductions in all-cause mortality under zero PfPR. Baseline all-cause mortality is supplied by IHME, not independently predicted by the DHS model.

Combine early/late neonatal inputs for the <1-month model effect, recording the day/month boundary approximation. Use direct matches for 1–5, 6–11 and 12–23 months. For ages 2–4, retain the user-selected **same IHME rate for all three bands and equal division of deaths/person-time**. Check that age inputs sum to under-five totals and that observed = attributable + counterfactual rates/counts. The former IGME-primary, regional death-share and 2025 carry-forward specifications are historical alternatives, not this calculation.

Use the current [country/age estimates](../results/cbh/primary_map_regional17_gamma2_v3/burden/country_age_estimates.csv), [country totals](../results/cbh/primary_map_regional17_gamma2_v3/burden/country_totals.csv) and [year totals](../results/cbh/primary_map_regional17_gamma2_v3/burden/year_summary.csv). [primary/02_effects.R](../R_cbh/primary/02_effects.R) applies the 17-variable splines to the same national MAP/IHME baseline inputs. Across 42 estimable countries, the current estimates are **973,935 deaths in 2005, 665,559 in 2015 and 565,616 in 2024**, compared with 854,154, 589,807 and 501,065 under the 18-variable adjustment and 973,148, 694,534 and 592,353 under the earlier individual adjustment. IHME estimates 564,067, 416,122 and 428,147 cause-specific malaria deaths in the same countries and years. All 45 country rows remain, with missing estimates for Cape Verde, Lesotho and São Tomé and Príncipe. The [country/year comparison](../results/cbh/primary_map_regional17_gamma2_v3/burden/comparison_country_totals.csv) preserves both estimates and their signed differences. National exposure and IHME baseline equality were verified; negative age contributions are retained, and age-specific intervals are not summed into total intervals.

Retain signed contributions, zero/current-prevalence support flags and conditional age-band intervals. National totals remain point estimates because cross-age sampling covariance is not estimated. Source, design, HIV-imputation, smoothing, transport and age-allocation uncertainty are not fully propagated. A nonlinear curve evaluated at national mean prevalence does not equal population-weighted subnational burden. Compare IHME malaria deaths only with explicit age/year/geographic alignment: cause-specific malaria deaths and the modeled reduction in all-cause mortality are different estimands, and export-release alignment remains unverified.

### 3.6 Secondary subnational burden: Nigerian states, 2024

Extend the national calculation of Section 3.5 to the **36 Nigerian states and the Federal Capital Territory for 2024**, as a secondary subnational extrapolation of the same primary MAP gamma=2 model. Nothing is refitted. For state s and band g:

\[
HR_{sg}^{0}=\exp\{f_g(0)-f_g(P_{s,2024})\},\quad
D_{sg}^{attr}=D_{sg}^{IHME}(1-HR_{sg}^{0}),\quad
D_{s}^{attr}=\sum_g D_{sg}^{attr}.
\]

**Inputs.** (i) IHME all-cause deaths and rates by GBD age group and state, both sexes, 2024, from the Data Explorer export dated 16 September 2026, stored with a hash and description in `data/external/nigeria_states/source.json`; state person-years are the implied count/rate ratio. (ii) The IHME under-5 malaria death rate per 100,000 by state for 2024, from the same export family, applied to the all-cause implied person-years to give the cause-specific comparator. (iii) Population-weighted state MAP PfPR₂–₁₀ for 2024 from `data/pfpr_admin1_ng_cd_2024.csv`. This is an inherited extraction (MAP admin-1 boundaries, GPW 2020 density weights **without** cell area); its producer is archived and it has not been re-derived with the national density-times-area method. Record this, do not silently re-extract.

**Age mapping.** Use exactly the national conventions: early and late neonatal deaths combined under the <1-completed-month effect; direct matches for 1–5 and 6–11 months; 12–23 months as the IHME "1 to 4" group minus the "2 to 4" group; and the "2 to 4" group's deaths and person-time divided equally across 24–35, 36–47 and 48–59 months with one shared rate. Every state's seven bands must reproduce its under-5 total, and the bands summed over states must reproduce the national 2024 age-band inputs used in Section 3.5 to within numerical precision; the calculation stops otherwise. That check ties the state export to the same IHME vintage as the national inputs.

**Outputs and checks.** [State-by-age estimates](../results/cbh/primary_map_regional17_gamma2_v3/nigeria_states/state_age_estimates_2024.csv) with signed attributable contributions, conditional intervals and support flags; [state totals](../results/cbh/primary_map_regional17_gamma2_v3/nigeria_states/state_totals_2024.csv) with the IHME malaria comparator and the model-to-IHME ratio; and a [reconciliation](../results/cbh/primary_map_regional17_gamma2_v3/nigeria_states/national_reconciliation_2024.csv) of the state sum against the national 2024 estimate. In the current version the state sum is 200,479 attributable deaths against 132,138 IHME malaria deaths (ratio 1.52), and differs from the national estimate of 200,988 by −0.25%. The two need not agree: the national figure evaluates each nonlinear curve at the national mean prevalence, whereas the state sum aggregates curve values at state prevalence, and the two exposure extractions differ as noted. Report the difference; it is expected to be small and it is not an error. All 37 state prevalences lie inside the central 95% of fitted exposure, so no state requires extrapolation beyond observed support. State totals are point estimates; band intervals are not summed. Calculation code: [burden/05_nigeria_state_burden.R](../R_cbh/burden/05_nigeria_state_burden.R).

**Interpretation.** The state comparison is diagnostic of *geography*, not of the national total. The model's attributable fraction varies with prevalence through the fitted age-band curves; monotonicity is not imposed. In the saved state comparison, IHME's malaria share of all-cause child deaths is highest in the low-prevalence southeast and lowest in the high-prevalence northwest, so the methods imply different spatial patterns of burden. As for the national comparison, cause-specific malaria deaths and modelled all-cause reductions are different estimands, the model shares IHME all-cause inputs, and agreement is not independent validation. A single national uplift to IHME for indirect malaria mortality cannot reconcile two different spatial patterns; note it as context, do not apply it in the main figure.

## 4. Stage 3 — plot saved results and separate primary from supplementary reporting

**Saved-output status (17-variable fit, 18 September 2026):** Figures 1–4, the inclusion flow, age-band descriptive/effect tables, primary diagnostic plots, and national/annual/state burden tables are regenerated from `primary_map_regional17_gamma2_v3` and exported to Overleaf. This reporting refresh does not refit Sahel, geography, period or other sensitivity analyses. Only components already in this plan are refreshed; historical covariate-forest and timing figures outside this primary reporting scope are not treated as current outputs.

| Reporting location | Content | Authoritative selection |
|---|---|---|
| Main results | Study flow, survey/sample coverage and seven age-band descriptive summaries. The flow shows every exclusion reason with its count: surveys without MAP geography; incomplete potential band; entry year outside 2000–2024; no regional MAP value; and required covariate unavailable, attributed to the first missing covariate in a declared order by [covariates/14_exclusion_attribution.R](../R_cbh/covariates/14_exclusion_attribution.R) so the reasons are disjoint | Full MAP sample and its exclusion ledger; `study_flow/` under current primary results |
| Main results: Figure 1 | Survey map and timing for all 124 registry surveys: the 95 included surveys as filled circles sized by contributing regions, and excluded surveys as distinct symbols by reason (no MAP geography; required covariates unavailable). All five MIS surveys are excluded for covariates, so the fitted sample is DHS only | `survey_map/survey_map_and_timing.png` under current primary results, exported as `fig1_survey_map_and_timing.png` |
| Main results: Figures 2–3 | Seven primary PfPR curves, X%→Y% contrasts and attributable share by age | `map_full`, gamma=2, from `results/cbh/primary_map_regional17_gamma2_v3/` |
| Main results: secondary burden | Country estimates and age contributions for 2005/2015/2024 using the primary model | Primary-only burden files linked above |
| Main results: Figure 4 | Three panels as deaths per 1,000 under-five child-years: 2024 country comparison with IHME (A), Nigerian state comparison (B) and annual 2000–2024 trend against IHME and UN IGME (C) | `burden_comparison/fig4_burden_comparison.png` under current primary results |
| Supplementary figure | Count version of Figure 4 (numbers of deaths, log axes) | `burden_comparison/sfig_burden_comparison_counts.png` |
| Supplementary figure: imputed covariates | PfPR curves from the complete-case primary and the imputed-covariate sensitivity version (Section 3.4.3), drawn from the saved comparison curves by [sensitivity/imputation/02_supplementary_figure.R](../R_cbh/sensitivity/imputation/02_supplementary_figure.R) with paper-facing labels; exported to `Supplementary Figures/sfig_pfpr_splines_imputed_covariates.png` with its caption | `results/cbh/primary_map_regional17_imputed_gamma2_v4/sfig_pfpr_splines_imputed_covariates.png` |
| Supplementary sensitivity results | Structure, geography, survey period, timing, design, missingness and other declared checks | Label old gamma=1 exploratory results separately from planned gamma=2 refits |
| Historical archive | Six-band person-time/count models, old joint fits and earlier burden outputs | [Archived plan](ANALYSIS_PLAN_BEFORE_MAP_GAMMA2.md), original result directories and code audit |

**Figure-version status:** the current paper artifacts and captions are in `primary_map_regional17_gamma2_v3/paper_figures/`; the previous versions remain preserved. Main Figures 1–4 and the supplementary inclusion flow are exported to the existing Overleaf figure locations by `R_cbh/reporting/export_paper.py`, which backs up replaced assets and verifies that no TeX file changes. Table sources are supplied as CSV, Markdown and `.latex.txt` fragments for author insertion because no `.tex` files may be edited. The age-band table includes observed-death percentages summing to 100.0%, both prevalence contrasts and no fit-time column. The national comparison table uses the same 42 countries and UN IGME source as Figure 4C; its ten-country display is ordered by absolute model–IHME difference, while its total includes all 42 countries. Full annual and state tables accompany Figure 4.

The paper's Figure 2 is the primary PfPR-by-age curve figure. **Figure 3** shows age bands on the x-axis and the malaria-attributable share of all-cause deaths **within each band** on the y-axis (0–100%), with four lines for PfPR of 10%, 20%, 30% and 40%. Calculate `100 × {1 − exp[f_g(0) − f_g(P)]}` from the saved primary MAP gamma=2 models, using zero prevalence as the counterfactual and the same hazard-based, fixed-person-time convention as the national burden analysis. This is not a distribution of total under-five deaths across ages. Connect the seven point estimates without implying a continuous-age fit; retain conditional intervals and zero-exposure extrapolation flags in the source table. [Figure](../results/cbh/primary_map_regional17_gamma2_v3/attributable_fraction_by_age.png), [estimates](../results/cbh/primary_map_regional17_gamma2_v3/attributable_fraction_by_age.csv), [code](../R_cbh/reporting/06_attributable_fraction_by_age.R).

**Figure 4** (decided 18 September 2026) is a single three-panel figure combining the former Figures 4, 6 and 5: two panels on top and one below, drawn by [reporting/11_burden_comparison_figure.R](../R_cbh/reporting/11_burden_comparison_figure.R) from saved aggregates only. **Every panel is expressed as malaria deaths per 1,000 under-five child-years**, so that the country and state panels are not dominated by population size; the same figure as numbers of deaths is a supplementary figure (below). All four paper figures omit titles, subtitles and embedded captions; separate caption text and filename mappings are in the [paper figure manifest](../results/cbh/primary_map_regional17_gamma2_v3/paper_figures/CAPTIONS.md). Preparing/copying figures does not edit manuscript TeX.

- **Panel A (top left)** is the 2024 country comparison with IHME: model-attributable and IHME cause-specific malaria deaths before age 5, each divided by the same under-five person-years implied by the IHME all-cause count and rate for that country (from `annual_comparison/country_estimates_2000_2024.csv`), on linear axes with identical limits from zero, an equality line, and labels for COD, NGA, AGO, UGA and TZA. All 42 estimable countries are shown; nothing is clipped. The separate 2005/2015/2024 three-year count scatter remains a saved result (`burden/country_vs_ihme.png`) but is no longer a paper figure.
- **Panel B (top right)** is the state-level analogue for Nigeria in 2024: the 36 states and FCT as rates on the state person-years implied by the IHME all-cause inputs, linear axes with identical limits from zero, an equality line, every state labelled, and points coloured by geopolitical zone for orientation only. It plots the saved [state totals](../results/cbh/primary_map_regional17_gamma2_v3/nigeria_states/state_totals_2024.csv) from Section 3.6 and nothing else. The caption states the national ratio and the reconciliation with the national estimate; the [table and audit](../results/cbh/primary_map_regional17_gamma2_v3/nigeria_states/README.md) give per-state ratios and the north–south pattern.
- **Panel C (bottom)** compares annual malaria-attributable under-five mortality rates per 1,000 child-years from the PfPR-ACM model with IHME cause-specific malaria and UN IGME, for 2000–2024. The legend must use “UN IGME”; the caption identifies the underlying CA-CODE 2026 release, which is distinct from the WHO World Malaria Report. Use the same 42 national-burden countries in every year. Recalculate national MAP exposure with population counts (density × cell area) and fixed GPW 2020 spatial weights. Apply the saved primary age-band effects to annual IHME all-cause deaths; preserve the equal-person-time assumption for ages 2–4 and signed age contributions. Sum deaths across countries and divide all three sources by the same annual under-five person-years inferred from the IHME all-cause under-five count/rate pair. Report rates per 1,000 child-years in the main figure (the saved tables keep per 100,000), not per live birth or an average of country rates. Use the direct under-five CA-CODE 2026 series from the official UN IGME/UNICEF portal; do not multiply all-age WHO totals by a constant child fraction. Input coverage is complete for all 1,050 country-years. The series was extended back from 2004 to 2000 on 21 September 2026: MAP rasters and the IHME all-cause and malaria exports already covered 2000, and the UN IGME CA-CODE extract was re-retrieved from the same UNICEF SDMX endpoint with `startPeriod=2000` (new `data/external/figure5/unicef_cacode_u5_malaria_2000_2024.csv`, superseded extract recorded in `source.json`). All 42 countries are complete in all three sources for 2000–2003, so the country set does not change across the longer series. Plot point estimates without summing marginal uncertainty bounds. [Panel data and audit](../results/cbh/primary_map_regional17_gamma2_v3/annual_comparison/README.md), [annual totals](../results/cbh/primary_map_regional17_gamma2_v3/annual_comparison/annual_totals_2000_2024.csv), [calculation code](../R_cbh/burden/04_annual_comparison.R).

The method shares IHME all-cause inputs, and attributable all-cause reductions and cause-specific comparator estimates are different estimands; the combined caption says so once. Within each country or state the model and comparator share a denominator, so the rate display changes which units are visually prominent, not the model-to-IHME ratios. The `PfPR-ACM model` label follows the naming rule in every panel. [Figure](../results/cbh/primary_map_regional17_gamma2_v3/burden_comparison/fig4_burden_comparison.png), [caption](../results/cbh/primary_map_regional17_gamma2_v3/burden_comparison/CAPTION.md). The paper figure manifest lists four figures.

**Supplementary count version of Figure 4:** the same three panels as numbers of deaths (`burden_comparison/sfig_burden_comparison_counts.png`, [caption](../results/cbh/primary_map_regional17_gamma2_v3/burden_comparison/CAPTION_counts.md)), exported to the manuscript's `Supplementary Figures/` folder. Panels A and B use true base-10 logarithmic scales with identical limits: **100 to 250,000 deaths** for countries and **200 to 30,000 deaths** for states. Points outside the display range on either axis (six small countries below 100 deaths) are clipped and listed in `burden_comparison/points_outside_display_limits.csv`, with all estimates retained. Panel C is the annual series per 100,000 child-years.

Plotting-only stages must read saved aggregate estimates and must not refit, change exposure definitions or select models according to available cache files. Label source, sample period, gamma, age bands, contrast and uncertainty. A main-results figure must select only the full MAP gamma=2 series. Keep links to authoritative saved artifacts instead of copying figures into competing locations.

## 5. Implementation status and next steps

**18 September plan revision, implemented and fitted:** `male_pct`, `multiple_birth_pct` and `mean_birth_order` are removed from the selected covariates and completeness checks; `mean_maternal_age_birth` is replaced with `mean_maternal_age_first_birth`; `wasting_pct` and `stunting_pct` are added. The availability audit verified `v212` distinct-mother weighting, used published WHO-standard anthropometry and reviewed geography rules, and reported missingness and survey-region losses after the unchanged fallback rules. `00_prepare_regional.R` consumes the audited overlay, asserts the new field set and saves the 17-variable dataset and scaling under `primary_map_regional17_gamma2_v3`; all seven bands were refitted and effects, burden and paper outputs regenerated from them. The same day the paper's Figures 4–6 were combined into one three-panel Figure 4 (Section 4) and the Snow comparison was archived. TeX files remain unchanged.

Definition sources: [DHS recode manual (`v212`)](https://dhsprogram.com/pubs/pdf/DHSG4/Recode7_DHS_10Sep2018_DHSG4.pdf), [DHS nutritional-status indicators](https://www.dhsprogram.com/data/Indicator-Explorer.cfm?indgrpid=194), and [DHS tabulation plan](https://dhsprogram.com/pubs/pdf/DHSM6/Final_Report_Tab_Plan_24Oct2014_DHSM6.pdf). These definitions do not establish data availability for each eligible survey.

**17 September regional refit (previous specification):** [run_regional.R](../R_cbh/primary/run_regional.R) implements the fitted 18-variable version, including preparation from existing audited inputs, seven fresh fits, effects, national/annual/state burden, aggregate diagnostics, iteration comparisons and current primary figures/tables. The new outputs and previous benchmark are kept separate.

**Previous-adjustment primary-only rerun:** [R_cbh/primary/](../R_cbh/primary/README.md) now separates fresh fitting, effect/burden calculation, aggregate diagnostic checks and MAP-only plotting. It regenerates the study inclusion flow and key-results index. The prepared dataset, HIV imputation, national exposure inputs and IHME inputs remain fixed; supplementary fits are not run. New outputs are saved separately from the preceding comparison workflow, with numerical differences documented in the [rerun report](../results/cbh/primary_map_gamma2_v1/REPORT.md).

**15 September audit and cleanup:** the [component-by-component audit](CODE_AUDIT.md) verified that the saved primary specification and data matched the plan at that date, before the 17 September regional-adjustment revision, while distinguishing unimplemented sensitivities and unresolved upstream issues. Sixty-eight superseded scripts were moved to [the archive](../archive/2026-09-15-code-audit/), along with preserved copies of three replaced entry points. Existing data, fitted objects and numerical results were not changed.

`Rscript run_all.R --primary` forces fresh 17-variable fits after verifying/assembling the audited regional input overlay. `Rscript R_cbh/primary/run_regional.R --resume` reuses only valid fit caches; `--report-only` uses saved fits for effects, diagnostics, comparisons, burden and figures. The runner performs no DHS source extraction, HIV refit, supplementary fitting or TeX-file writes. It calculates primary annual/state burden, tables and all plan-defined primary figures, including the combined Figure 4; copying the verified paper assets to Overleaf is a separate export step (`python3 R_cbh/reporting/export_paper.py --destination PATH --copy`). LaTeX table source is saved as plain text. The older `primary/run.R` explicitly selects the legacy version; do not use it for current primary results. Table writers now produce `.latex.txt` source instead of writing TeX files. `--audit` verifies the preserved older snapshot; `--check-inputs` checks local source prerequisites.

**17 September subnational extension:** the Nigerian state calculation and its figure (Section 3.6, now Figure 4B) are implemented as two stages in the primary runner, after the annual comparison and before the combined-figure and paper-manifest stages. They read only saved primary components, the state-level IHME exports under `data/external/nigeria_states/` and the inherited state MAP extraction, and they write under `results/cbh/primary_map_regional17_gamma2_v3/nigeria_states/`. The previous versions remain preserved. The inherited state exposure extraction is a documented input-stage gap alongside the national baseline tables.

**Remaining command-routing limitation:** older [sensitivity fitting](../R_cbh/sensitivity/01_fit.R) and [burden defaults](../R_cbh/burden/settings.R) still refer to gamma=1-era fits. `analysis/01_fit_complete_case.R` should be used with `--prepare-only` for dataset preparation; its default fit is historical. These scripts remain because they provide reference frames/knots or input tables used by the selected workflow. Do not use those defaults as a gamma=2 primary run. The new primary-only pipeline bypasses these defaults. National burden still reads explicit baseline columns from the earlier saved national input tables; consolidating their upstream producers remains a data-stage task.

Retain the three-stage workflow: **make datasets → fit declared models and calculate effects → plot saved results**. Priorities are:

1. Bring the manuscript text, tables and captions into line with the fitted 17-variable results (sample sizes, contrasts, burden totals, the neonatal association, the combined Figure 4). Preserve the 18-variable fits and figures as the benchmark; they must not be described as results from the current specification. No TeX files are edited by this workflow. In-sample fitted-outcome checks supplement numerical diagnostics but are not cross-validation or influence analysis.
2. Complete the remaining primary-compatible gamma=2 sensitivity fits on the 17-variable specification: joint versus separate ages, the same-sample fit omitting wasting and stunting, and the observed-regional-only fit without fallbacks. The Sahel, Eastern Africa and survey-period subgroup refits (Section 3.4.1) and the imputed-covariate version with its multiple-imputation check (Section 3.4.3) are done. Decide after review whether the imputed-covariate version should become the primary. Preserve historical comparisons with their actual settings.
3. Resolve the [open data and inference issues](OPEN_ANALYSIS_ISSUES.md), especially regional exposure coverage, missing-data selection, calendar/age coding, imputation uncertainty and survey/within-child dependence.
4. Consolidate the independent data build and prediction interfaces. Rebuild from a declared input snapshot and verify cache hashes. Fresh primary fitting now uses prepared data and committed reference knots; baseline national input tables still need an independent producer interface.
5. Finish separating input-only functions from the retained legacy bridges, then archive their remaining surplus branches. The 15 September cleanup removed the superseded independent modelling/plotting scripts; still-needed producers were retained with explicit reasons. Keep historical code/results traceable and do not execute legacy external-copy actions during migration.

Completion requires auditable survey accounting, validated child-band eligibility and joins, consistent primary specification and artifact selection, reviewed diagnostics, declared uncertainty and reproducible main/supplementary figures. Historical six-band/count-model choices are preserved in the archive and must not override this plan.
